<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="stta.ac.id.simulasimodeacradar">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_radar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_radar"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutActivity"></activity>
        <activity android:name=".ModeCActivity" />
        <activity android:name=".ModeAActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreenActivity"
            android:theme="@style/AppThemeSplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>